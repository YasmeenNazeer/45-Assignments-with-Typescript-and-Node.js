"use strict";
let userNames = ['admin', 'fiza', 'misbah', 'laiba', 'barra'];
if (userNames.length == 0) {
    console.log('We need to find some users!');
}
else {
    userNames = [];
    console.log('All users have been removed.  ' + userNames.length);
}
